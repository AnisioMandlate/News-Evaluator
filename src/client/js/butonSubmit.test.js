import { buttonSubmit } from "./buttonSubmit";

describe('Test, the function "buttonSubmit()" should exist', () => {
  test("It should return true", async () => {
    expect(buttonSubmit).toBeDefined();
  });
});
